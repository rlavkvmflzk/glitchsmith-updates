{
  "latestVersion": "0.6.0",
  "downloadUrl": "",
  "notes": "<b>v0.6.0 Update</b><br>✨ <b>Notification Board:</b> GlitchSmith updates and announcements now surface through a single board instead of one popup per module. The board uses tabs for Updates and Announcements, with per-module sub-tabs when more than one entry is available. Skip, disable, and acknowledge actions stay per module.<br>✨ <b>Library self-notifications:</b> The library now publishes its own update entries through the same board.<br>✨ <b>Notifier API:</b> Added <code>api.notifier.openBoard()</code> for modules that want to surface the board manually.<br><br><b>v0.5.1 Update</b><br>⚡ <b>SWADE decimal currency migration:</b> Existing SWADE worlds whose currency entry was created before decimal support automatically gain decimal precision on next load so that <code>system.details.currency</code> accepts fractional amounts without a manual reset. Runs once per world and skips currencies the GM has already configured.<br><br><b>v0.5.0 Update</b><br>✨ <b>Shared sheet currency API:</b> Added <code>getSheetCurrencies</code>, <code>getSheetBalance</code>, <code>setSheetBalance</code>, <code>modifySheetBalance</code>, <code>setSheetBalances</code>, and <code>registerSheetCurrencyDriver</code> under <code>glitchsmith-lib.currency</code>.<br>✨ <b>Batch sheet currency updates:</b> Added <code>modifySheetBalances</code> for multi-denomination rewards and transactions.<br>✨ <b>Built-in sheet drivers:</b> Actor data paths, PF2e/SF2e coin inventory APIs, and WFRP4e money items.<br>✨ <b>Driver registration hook:</b> Added the <code>glitchsmith-lib.registerSheetCurrencyDrivers</code> hook for external systems.",
  "critical": false,
  "forceResetId": null,
  "announcement": {
    "id": "glitchsmith-lib-v0.6.0-release",
    "show": true,
    "title": "🔔 GlitchSmith Library v0.6.0",
    "content": "<p><b>GlitchSmith Library v0.6.0 is ready.</b></p><ul><li><b>Notification Board:</b> Updates and announcements from GlitchSmith modules now appear in one tabbed board instead of separate popups per module. Per-module skip, disable, and acknowledge controls are preserved.</li><li><b>Library self-notifications:</b> The library now publishes its own entries through the same board.</li><li><b>SWADE decimal migration (0.5.1):</b> Existing SWADE worlds get decimal precision on the built-in currency entry without a manual reset.</li></ul>"
  }
}
